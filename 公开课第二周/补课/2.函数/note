1.创建一个对象数据类型需要三步：
1）开辟一个空间地址
2）把对象的属性名和属性值放在这个内存空间里；
3）把空间地址赋值给变量名；
2.堆内存和栈内存
栈内存：提供了一个供JS代码执行的环境； 包含全局作用域window和私有作用域；
       要做的事情：1）供JS代码执行的环境 2）把基本数据类型都放在栈内存里；
堆内存：存的都是引用数据类型的值；
       对象数据类型：在堆内存中存的是 属性名和属性值；
       函数数据类型：在堆内存中存的是字符串；
3.关于函数的定义和调用阶段：
 函数定义三阶段：
    1.开辟一个空间地址
    2.把函数体中的所有JS代码做为字符串存放在堆内存中；
    3.把空间地址赋值给函数名；
 函数执行两阶段
    1.形成一个私有作用域；
    2.把堆内存中的字符串做为JS代码来执行；
4.函数传参；
    1.函数如果定义了形参，但是调用时没有传值，我们拿到的参数是undefined;
    2.函数的参数可以是任意数据类型；
    3.当参数不确定的时候，不需要写形参，可以使用函数自带的参数机制：arguments；
5.return：
    1）返回值：
    返回值在什么情况下为undefined?
    1.函数执行完，里面没有写return；
    2.写了return但是没有赋值；
    2）阻断程序执行的作用；
    目的：让我们人为的控制让代码执行到哪个阶段；
6.闭包：函数被调用的时候形成一个私有作用域，保护里面的变量不受外界的干扰，函数的这种保护机制，叫做闭包；
.......................................
7.以上我们所说的都是有名字的函数；
函数： 有名字的函数 和 匿名函数；
匿名函数包括：1）函数表达式 2）自执行函数；
自执行函数:就是函数自己调用自己
自执行函数的5种写法：
(function(){})();
~function(){}();
+function(){}();
-function(){}();
！function(){}();
8.如何实现封装
1）保证HTML结构一致；让里面所有元素取都在同一个父级下去获取；
2）先实现一个选项卡的功能；
3）封装：把可能会变得值作为变量；
4）复制或者修改多个选项卡结构；


